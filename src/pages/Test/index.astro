<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auto Updating Span</title>
    <style>
        .custom-span {
            position: relative;
            width: 80%;
            height: 80%;
        }

        #auto-update-span {
            display: block;
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: var(--top-nav-bg-color);
            color: var(--nav-txt-color);
        }

        table {
            margin-bottom: 20px;
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        input[type="color"] {
            width: 50px;
        }

        input[type="number"] {
            width: 50px;
        }

        button {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="custom-span">
        <span id="auto-update-span"></span>
    

    <table>
        <tr>
            <th>Variable Name</th>
            <th>Input Value</th>
        </tr>
        <tr>
            <td><strong>--top-nav-bg-color</strong></td>
            <td><input type="color" id="top-nav-bg-color" value="#00008c"></td>
        </tr>
        <tr>
            <td><strong>--nav-bg-color</strong></td>
            <td><input type="color" id="nav-bg-color" value="#000080"></td>
        </tr>
        <tr>
            <td><strong>--nav-txt-color</strong></td>
            <td><input type="color" id="nav-txt-color" value="#ffffff"></td>
        </tr>
        <tr>
            <td><strong>--header-bg-color</strong></td>
            <td><input type="color" id="header-bg-color" value="#f0f2f2"></td>
        </tr>
        <tr>
            <td><strong>--main-content-color</strong></td>
            <td><input type="color" id="main-content-color" value="#f5deb3"></td>
        </tr>
    </table>

    <button onclick="updateSpan()">Update</button>
    </div>
    <script lang="js">
        function updateSpan() {
            const topNavBgColor = document.getElementById('top-nav-bg-color').value;
            const navBgColor = document.getElementById('nav-bg-color').value;
            const navTxtColor = document.getElementById('nav-txt-color').value;
            const headerBgColor = document.getElementById('header-bg-color').value;
            const mainContentColor = document.getElementById('main-content-color').value;

            document.documentElement.style.setProperty('--top-nav-bg-color', topNavBgColor);
            document.documentElement.style.setProperty('--nav-bg-color', navBgColor);
            document.documentElement.style.setProperty('--nav-txt-color', navTxtColor);
            document.documentElement.style.setProperty('--header-height', headerHeight);
            document.documentElement.style.setProperty('--header-bg-color', headerBgColor);
            document.documentElement.style.setProperty('--main-content-color', mainContentColor);

            const span = document.getElementById('auto-update-span');
            span.style.width = '80%';
            span.style.height = '80%';
            span.style.backgroundColor = topNavBgColor;
            span.style.color = navTxtColor;
        }
    </script>
</body>
</html>
